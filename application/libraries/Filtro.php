<?php
defined('BASEPATH') OR exit('No direct script access allowed');

class Filtro {

    public function Reading($ticket){

    	$data = array();


    	// $data->TicketID = $ticket->TicketID;
    	// $data->CategoryID = $ticket->CategoryID;
    	// $data->CustomerID = $ticket->CustomerID;
    	// $data->CustomerName = $ticket->CustomerName;
    	// $data->CustomerEmail = $ticket->CustomerEmail;
    	// $data->CustomerEmail = $ticket->CustomerEmail;
    	// $data->CustomerEmail = $ticket->CustomerEmail;

    	// $data->$Subject = $this->Token($ticket->Subject);
    	// $data->$Message = $this->Token($ticket->Message);
    	// $data->DateCreate = $ticket->DateCreate;
    	// $data->Sender = $this->Token($ticket->Sender);
    	
    	//echo "<br><br>";
    	die();

    }
}